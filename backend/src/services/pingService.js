// src/services/pingService.js

// Se exporta una función llamada getPingMessage
// Esta función no recibe parámetros y retorna un string: 'pong'
exports.getPingMessage = () => {
    return 'pong';
};